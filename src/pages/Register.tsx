export function Register() {
  return (
    <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-24 min-h-[50vh] flex items-center justify-center">
      {/* TODO: Build clean, minimalist auth forms using Shadcn UI components. */}
    </div>
  );
}
